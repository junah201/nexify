# `APIRouter` class

Here's the reference information for the `Route` class, with all its parameters, attributes and methods.

You can import the `APIRouter` class directly from `nexify`:

```python
from APIRouter import Nexify
```

::: nexify.routing.APIRouter
